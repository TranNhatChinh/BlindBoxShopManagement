<Window x:Class="BlindBoxShopManagement.AddStaffWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlindBoxShopManagement"
        mc:Ignorable="d"
        Title="AddStaffWindow" Height="500" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Add New Staff" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Form Grid -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left Column -->
            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                <TextBlock Text="Username"/>
                <TextBox x:Name="txtUsername" Margin="0,5"/>

                <TextBlock Text="Password"/>
                <PasswordBox x:Name="txtPassword" Margin="0,5"/>

                <TextBlock Text="Full Name"/>
                <TextBox x:Name="txtFullName" Margin="0,5"/>

                <TextBlock Text="Email"/>
                <TextBox x:Name="txtEmail" Margin="0,5"/>

                <TextBlock Text="Phone"/>
                <TextBox x:Name="txtPhone" Margin="0,5"/>

                <TextBlock Text="Address"/>
                <TextBox x:Name="txtAddress" Margin="0,5"/>
            </StackPanel>

            <!-- Right Column -->
            <StackPanel Grid.Column="1">
                <TextBlock Text="Gender"/>
                <ComboBox x:Name="cbGender" Margin="0,5" SelectedIndex="0">
                    <ComboBoxItem Content="Nam"/>
                    <ComboBoxItem Content="Nữ"/>
                </ComboBox>

                <TextBlock Text="Date of Birth"/>
                <DatePicker x:Name="dpDOB" Margin="0,5"/>

                <TextBlock Text="Identity Number"/>
                <TextBox x:Name="txtIdentity" Margin="0,5"/>

                <TextBlock Text="Avatar URL"/>
                <TextBox x:Name="txtAvatar" Margin="0,5"/>
            </StackPanel>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Width="100" Margin="10,0" Click="btnSave_Click"/>
            <Button Content="Cancel" Width="100" Margin="10,0" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
