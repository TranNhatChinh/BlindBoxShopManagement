<Window x:Class="BlindBoxShopManagement.StaffManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlindBoxShopManagement"
        mc:Ignorable="d"
        Title="Staff Management" Height="500" Width="900" WindowStartupLocation="CenterScreen" Loaded="Window_Loaded">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Staff Management"
                   FontSize="30"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,10"/>

        <!-- Search + Buttons -->
        <DockPanel Grid.Row="1" Margin="0,10,0,10">
            <TextBox x:Name="txtSearch"
                     Width="200"
                     Height="30"
                     VerticalAlignment="Center"
                     Margin="0,0,10,0"
                     VerticalContentAlignment="Center"
                     Text="Search by Full Name"
                     Foreground="Gray"
                     GotFocus="txtSearch_GotFocus"
                     LostFocus="txtSearch_LostFocus"/>
            <Button Content="Search" Width="80" Height="30" Margin="0,0,20,0" Click="btnSearch_Click"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Add" Width="80" Margin="5,0" Click="btnAdd_Click"/>
                <Button Content="Edit" Width="80" Margin="5,0" Click="btnEdit_Click"/>
                <Button Content="Delete" Width="80" Margin="5,0" Click="btnDelete_Click"/>
            </StackPanel>
        </DockPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="dgvDisplay"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0,0,0,10">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Account ID" Binding="{Binding AccountId}" Width="*"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="*"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
                <DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="*"/>
                <DataGridTextColumn Header="DOB" Binding="{Binding DateOfBirth}" Width="*"/>
                <DataGridTextColumn Header="ID Number" Binding="{Binding IdentityNumber}" Width="*"/>
                <DataGridTextColumn Header="Avatar" Binding="{Binding Avatar}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
